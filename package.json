{
  "name": "interpoll-vanilla",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A peer-to-peer polling and forum app built on GenosDB — vanilla JavaScript, no UI framework.",
  "scripts": {
    "dev": "bun dev.js",
    "build": "rm -rf dist && bun build ./index.html --outdir dist --minify --sourcemap=linked --public-path=/ && bun scripts/copy-genosdb.js dist",
    "serve": "bun server.js"
  },
  "dependencies": {
    "genosdb": "0.16.0"
  }
}
